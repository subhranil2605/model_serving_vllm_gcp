[project]
name = "model-serving-vllm-gcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["langchain>=1.3.2"]

[dependency-groups]
dev = ["python-dotenv>=1.2.2", "ruff>=0.15.15"]

[tool.pytest.ini_options]
pythonpath = ["."]

[tool.setuptools]
packages = ["client"]
